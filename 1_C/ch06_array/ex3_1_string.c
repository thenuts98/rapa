#include <stdio.h>

int main(void){
    //char name[] = "jenny";
    char name[] = {'j', 'e', 'n', 'n', 'y', '\0'};
    printf("%s", name);
    // for(int idx = 0; idx < 6; idx++){
    //     printf("index : %d : 글자 : %c\n", idx, name[idx]);
    // }
}